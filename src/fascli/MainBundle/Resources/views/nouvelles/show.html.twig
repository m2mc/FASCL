{% extends '::appLayout.html.twig' %}

{% block sidebar %}
  {% if is_granted("ROLE_USER") %}
              <li class="active"><a href="#">INFORMATIONS</a></li>
              <li><a href="{{ path('nouvelles') }}">NOUVELLES</a></li>
              <li><a href="{{ path('memos') }}">MEMOS</a></li>
              <li><a href="{{ path('reg') }}">REGLEMENTS & LOIS</a></li>
              <li><a href="#"></a></li>
              {% endif %}
{% endblock %}


{% block table -%}
    <h1>NOUVELLE</h1>

    <table class="table table-striped table-bordered table-condensed table-hover" style="font-family : Verdana;font-size:1.2em;text-align:justify;">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>DATE D'EDITION</th>
                <td>{{ entity.datenr|date('d-m-Y') }}</td>
            </tr>
            <tr>
                <th>TITRE</th>
                <td>{{ entity.titre|raw }}</td>
            </tr>
            <tr>
                <th>CONTENU</th>
                <td>{{ entity.contenu|raw }}</td>
            </tr>
            <tr>
                <th>AUTEUR</th>
                <td>{{ entity.enregistreur }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="unstyled pager">
    <li>
        <a href="{{ path('nouvelles') }}">
            RETOUR
        </a>
    </li>
    {#<li>
        <a href="{{ path('nouvelles_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>#}
    {#<li>{{ form(delete_form) }}</li>#}
</ul>
{% endblock %}
